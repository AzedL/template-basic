{
  "name": "{{name}}",
  "description": "{{description}}",
  "author": "Azed",
  "private": true,
  "license": "MIT",
  "scripts": {
    "azg": "git init && az-git -n={{name}} -d=\"{{description}}\""
  }
}
